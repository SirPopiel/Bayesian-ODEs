FILO:

- Estendere l'algoritmo di Pernikaris a sistemi dinamici solo parzialmente osservabili.

- Legare tra loro diversi sistemi dinamici con la loro dinamica interna (viaggi) e valutare influenza. Termine di interazione non quantificabile -> rete neurale.

- Oppure, backpropagation insieme dei diversi sistemi dinamici -> ma cercare di renderlo il più veloce possibile.

- Usare siamese neural networks per allenamento dei modelli insieme, vedendoli come un unico modello per il covid traslato.

- In qualche modo, 'informare' il modello dinamico delle misure in atto (lockdown, chiusura ecc) -> studiare se possono essere considerati sistemi dinamici diversi o meno.

- Time-varying parameter identification in dynamical systems. (i parametri di un modello covid cambiano! Considerare un meta-modello che, in base ai cambiamenti dei parametri, identifichi cambiamenti (e.g. lockdown) dei parametri del modello e divida in 'fasi' il tutto o dia come output una curva temporale dei parametri (con relativa incertezza? Oppure curva senza, ma poi si mostra come effettivamente la varianza sulla stima del parametro sia alta)